public class Validator {

    //constructor
    public Validator() {}

    public boolean validate(Token[] tokens) {
        /*
            assumptions:
                -if is followed by else
                -if is followed by then
                -
         */

        return true;
    } //validate
}
